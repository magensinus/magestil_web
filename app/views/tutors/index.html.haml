// Index

- unless params[:course_id]
  - content_for :theme_class do
    = "set-theme-#{@tutors.slug}"

= render "breadcrumbs"
= render "tutors", object: @tutors, parent: @academy_course, objects: @academy_course_tutors

- unless params[:course_id]
  %hr.separator.medium
  = render "documents"

- if params[:course_id]
  - content_for :theme_class do
    = "set-theme-#{@academy_course.slug}"
  - content_for :theme_aside_class do
    = "set-theme-aside-#{@academy_course.slug}"
  = render "lib/components/courses/menu_aside", grandparent: @academy_category, parent: @academy_course, objects: @academy_course_sections
  = render "section_theme", grandparent: @academy_category, parent: @academy_course, object: @academy_course
