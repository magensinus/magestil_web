// Breadcrumbs

- content_for :breadcrumbs do
  .breadcrumbs
    %ul.breadcrumbs-box
      %li.breadcrumbs-item
        = link_to "Voltar", course_path(@academy_course), class: "link mono tiny", style: "color: #{parent.font_color_one}"
